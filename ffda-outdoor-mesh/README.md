# ffda-outdoor-mesh

This package allows to activate a different outdoor mode.

It preserves the mesh-functionality by using a single outdoor allowed channel instead of using ACS to select a channel. This means the 5GHz mesh will become unavailable while the outdoor-mesh is active, eliminating the need for a coordinated channel-switch.
